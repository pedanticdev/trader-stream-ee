# TradeStreamEE Environment Variables

# JVM Options for Azul Platform Prime
# Adjust these for your performance requirements
JAVA_OPTS=-XX:+UseZGC -Xms512m -Xmx2g -Xlog:gc*:file=/opt/payara/gc.log:time,uptime:filecount=5,filesize=10M -Djava.net.preferIPv4Stack=true

# Application Settings
APP_PORT=8080
APP_CONTEXT_ROOT=trader-stream-ee

# Aeron Configuration (embedded in application)
AERON_CHANNEL=aeron:ipc
AERON_STREAM_ID=1001

# Performance Tuning Notes:
# -Xms: Initial heap size (match -Xmx for consistent performance)
# -Xmx: Maximum heap size (increase for more throughput)
# -XX:+UseZGC: Enable ZGC (low-latency garbage collector)
# -Xlog:gc*: GC logging configuration
