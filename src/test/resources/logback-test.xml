<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console Appender for Test Output -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender for Test Logs (optional) -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>target/test.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>target/test.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Log Level Configuration for Tests -->
    <!-- Reduce noise during tests, but keep important information -->

    <!-- Application Logging -->
    <logger name="fish.payara.trader" level="INFO"/>

    <!-- Reduce third-party noise during tests -->
    <logger name="io.aeron" level="WARN"/>
    <logger name="jakarta.enterprise" level="WARN"/>
    <logger name="org.glassfish" level="WARN"/>
    <logger name="org.eclipse" level="WARN"/>

    <!-- GC and monitoring logs at INFO for visibility in tests -->
    <logger name="fish.payara.trader.gc" level="INFO"/>
    <logger name="fish.payara.trader.pressure" level="INFO"/>
    <logger name="fish.payara.trader.monitoring" level="INFO"/>

    <!-- Root Logger -->
    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
        <!-- Uncomment to also log to file during tests -->
        <!-- <appender-ref ref="FILE"/> -->
    </root>

</configuration>